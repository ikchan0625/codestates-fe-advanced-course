import styled from "styled-components";

function Pagenation({ pageLength, limit, page, setPage }) {
  const length = Math.floor(pageLength / limit);

  return (
    <>
      <div>hello</div>
    </>
  );
}
export default Pagenation;
